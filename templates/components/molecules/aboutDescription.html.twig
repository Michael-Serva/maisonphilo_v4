<a href={{ path(href) }} class="text-center">
    <div class="text-success-800 container">
        <span class="display-5 fw-bold d-block">
            {{title | trans}}
        </span>
        <span class="fs-1 text-muted">
            {{content | trans}}
        </span>
    </div>
   {% block content %}{% endblock %}
</a>
